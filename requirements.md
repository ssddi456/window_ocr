本工程用于创建一个录制和回放用户操作的工具，主要功能包括：

1. 启动时展示ui，点击选择窗口按钮后，用户可以选择windows系统上的任意窗口
2. 点击开始录制按钮后，工具会创建session，开始捕获用户在选定窗口上的鼠标点击操作
3. 点击信息包含鼠标点击相对绝对位置、点击类型（左键、右键等）以及时间戳以及截图等信息，并将这些信息保存到session, 记录到本地文件中
4. 点击停止录制按钮后，工具会停止捕获用户操作
5. 点击回放按钮后，工具会读取之前保存的session文件，并按照记录的信息在选定窗口上模拟鼠标点击操作
